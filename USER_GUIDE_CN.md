# Auto Recorder 使用指南 📖

## 目录

1. [安装说明](#安装说明)
2. [Windows 特殊配置](#windows-特殊配置)
3. [基本使用](#基本使用)
4. [高级配置](#高级配置)
5. [常见问题](#常见问题)
6. [使用场景](#使用场景)

## 安装说明

### 方法一：下载预编译版本（推荐）

1. 访问 [Releases 页面](https://github.com/yourusername/auto-recorder/releases)
2. 下载适合您系统的版本：
   - Windows 64位: `auto-recorder-windows-x64.exe`
   - Windows 32位: `auto-recorder-windows-x86.exe`
   - Linux 64位: `auto-recorder-linux-x64`
   - macOS Intel: `auto-recorder-macos-x64`
   - macOS Apple Silicon: `auto-recorder-macos-arm64`

### 方法二：从源码编译

```bash
# 1. 安装 Rust（如果还没有）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 2. 克隆仓库
git clone https://github.com/yourusername/auto-recorder.git
cd auto-recorder

# 3. 编译
cargo build --release

# 4. 运行
./target/release/auto-recorder --help
```

## Windows 特殊配置

**重要：Windows 用户必须先启用"立体声混音"才能录制扬声器音频！**

### 启用立体声混音的步骤

#### Windows 10/11:

1. **打开声音设置**
   - 右键点击任务栏右下角的音量图标
   - 选择"声音设置"或"打开声音设置"

2. **进入高级声音设置**
   - 向下滚动，点击"高级声音选项"
   - 或点击"更多声音设置"

3. **启用立体声混音**
   - 切换到"录制"标签页
   - 在空白处右键，确保勾选"显示已禁用的设备"和"显示已断开的设备"
   - 找到"立体声混音"（Stereo Mix）、"您听到的声音"（What U Hear）或类似名称的设备
   - 右键点击该设备，选择"启用"

4. **设为默认设备（可选）**
   - 右键点击已启用的立体声混音设备
   - 选择"设为默认设备"

#### 常见设备名称：

- 中文系统：立体声混音、您听到的声音
- 英文系统：Stereo Mix, What U Hear, Wave Out Mix
- Realtek：立体声混音 (Realtek High Definition Audio)
- 其他声卡品牌会有不同的命名

#### 如果找不到立体声混音设备：

某些笔记本电脑或声卡可能不支持立体声混音功能。您可以：

1. **更新声卡驱动**
   - 访问声卡制造商网站下载最新驱动
   - 或使用 Windows 更新

2. **使用虚拟音频设备**
   - 安装 VB-Audio Virtual Cable 或 VoiceMeeter
   - 这些软件可以创建虚拟音频设备

## 基本使用

### 1. 首次运行 - 生成配置文件

```bash
# Windows
auto-recorder.exe gen-config

# Linux/macOS
./auto-recorder gen-config
```

这会创建 `config.json` 文件，包含默认设置。

### 2. 查看可用音频设备

```bash
auto-recorder list-devices
```

输出示例：
```
=== Input Devices ===
  - Microphone (Realtek High Definition Audio)
  - 立体声混音 (Realtek High Definition Audio)

=== Output Devices ===
  - Speakers (Realtek High Definition Audio)
  - Headphones (USB Audio Device)
```

### 3. 手动录音

**基础录音：**
```bash
# 使用默认设置开始录音
auto-recorder record

# 按 Ctrl+C 停止录音
```

**自定义参数录音：**
```bash
# 高质量录音
auto-recorder record --sample-rate 48000 --bit-rate 320 --quality 0

# 指定输出目录
auto-recorder record --output ./my-recordings

# 组合使用
auto-recorder record -s 48000 -b 192 -q 1 -o ./calls
```

### 4. 自动录音模式

```bash
# 启动自动监控
auto-recorder auto

# 程序将持续运行，监控通话应用
# 检测到通话时自动开始录音
# 通话结束后自动停止并保存
```

**带详细日志：**
```bash
auto-recorder --verbose auto
```

## 高级配置

### 配置文件详解

编辑 `config.json`：

```json
{
  // 录音保存目录
  "output_dir": "recordings",
  
  // 采样率 (Hz)
  // 22050 - 低质量，小文件
  // 44100 - CD 质量（推荐）
  // 48000 - 专业质量
  "sample_rate": 44100,
  
  // 比特率 (kbps) - 保留字段，WAV格式不使用
  // 用于将来MP3支持或外部转换参考
  "bit_rate": 128,
  
  // 质量 (0-9) - 保留字段，WAV格式不使用
  // 用于将来MP3支持或外部转换参考
  "quality": 2,
  
  // 是否启用自动录音
  "auto_recording": true,
  
  // 要监控的应用程序列表
  "monitored_apps": [
    "WeChat.exe",      // 微信
    "QQ.exe",          // QQ
    "Lark.exe",        // 飞书（国际版）
    "feishu.exe",      // 飞书
    "Skype.exe",       // Skype
    "Teams.exe",       // Microsoft Teams
    "Zoom.exe",        // Zoom
    "Discord.exe",     // Discord
    "DingTalk.exe",    // 钉钉
    "YourApp.exe"      // 添加您需要的应用
  ],
  
  // 静音检测阈值 (0.0-1.0)
  // 数值越小越敏感
  "silence_threshold": 0.01,
  
  // 静音持续多少秒后停止录音
  "silence_duration": 3
}
```

### 质量预设

#### 语音通话（节省空间）
```json
{
  "sample_rate": 22050,
  "bit_rate": 64,
  "quality": 5
}
```
预计文件大小：约 0.5 MB/分钟

#### 标准质量（推荐）
```json
{
  "sample_rate": 44100,
  "bit_rate": 128,
  "quality": 2
}
```
预计文件大小：约 1 MB/分钟

#### 高质量（音乐级别）
```json
{
  "sample_rate": 48000,
  "bit_rate": 320,
  "quality": 0
}
```
预计文件大小：约 2.4 MB/分钟

## 常见问题

### ❓ 录音文件为空或很小

**可能原因和解决方案：**

1. **未启用立体声混音**
   - 参考上文 [Windows 特殊配置](#windows-特殊配置)

2. **音量太小**
   - 检查系统音量设置
   - 调整 `silence_threshold` 为更大的值（如 0.05）

3. **选错了音频设备**
   - 运行 `auto-recorder list-devices` 查看可用设备
   - 确保立体声混音设备已启用

### ❓ 检测不到通话应用

**解决方案：**

1. **检查进程名称**
   ```bash
   # 打开 Windows 任务管理器
   # 切换到"详细信息"标签
   # 找到通话应用的进程名（如 WeChat.exe）
   ```

2. **更新配置文件**
   ```json
   "monitored_apps": [
     "实际的进程名.exe"
   ]
   ```

3. **某些应用使用多个进程**
   - 可能需要添加所有相关进程
   - 例如 Teams 可能有 Teams.exe 和 Teams_helper.exe

### ❓ Linux/macOS 自动录音不工作

**说明：**

自动录音功能目前仅支持 Windows。Linux 和 macOS 用户只能使用手动录音模式。

未来版本可能会添加对其他平台的支持。

### ❓ 录音有杂音或断断续续

**解决方案：**

1. **降低采样率**
   ```bash
   auto-recorder record --sample-rate 22050
   ```

2. **关闭其他占用音频的程序**

3. **更新声卡驱动**

4. **使用有线耳机**（而非蓝牙）

### ❓ 如何只录制麦克风或扬声器

目前程序会混合录制两者。如果只需要单一音源：

1. 在 Windows 声音设置中禁用不需要的设备
2. 或修改源代码中的混音逻辑

## 使用场景

### 场景 1：商务通话记录

**配置：**
```json
{
  "sample_rate": 44100,
  "bit_rate": 128,
  "quality": 2,
  "monitored_apps": ["Teams.exe", "Zoom.exe", "Skype.exe"],
  "silence_duration": 5
}
```

**使用：**
```bash
auto-recorder auto
```

程序会自动检测并录制所有商务通话。

### 场景 2：远程面试

**配置：**
```json
{
  "sample_rate": 48000,
  "bit_rate": 192,
  "quality": 1,
  "silence_duration": 10
}
```

**使用：**
```bash
# 面试前手动启动
auto-recorder record --output ./interviews
```

### 场景 3：播客录制

**配置：**
```json
{
  "sample_rate": 48000,
  "bit_rate": 320,
  "quality": 0
}
```

**使用：**
```bash
auto-recorder record -s 48000 -b 320 -q 0 -o ./podcast
```

### 场景 4：客服质检

**配置：**
```json
{
  "monitored_apps": [
    "客服系统.exe",
    "Skype.exe"
  ],
  "auto_recording": true,
  "output_dir": "D:/客服录音"
}
```

**使用：**
```bash
# 开机自动启动
auto-recorder auto
```

## 命令行快速参考

```bash
# 生成配置文件
auto-recorder gen-config

# 列出音频设备
auto-recorder list-devices

# 自动录音模式
auto-recorder auto
auto-recorder --verbose auto              # 详细日志
auto-recorder --config custom.json auto   # 自定义配置

# 手动录音
auto-recorder record
auto-recorder record -s 48000             # 指定采样率
auto-recorder record -b 192               # 指定比特率
auto-recorder record -q 0                 # 指定质量
auto-recorder record -o ./output          # 指定输出目录
auto-recorder record -s 48000 -b 320 -q 0 # 组合参数

# 帮助
auto-recorder --help
auto-recorder record --help
```

## 法律声明

⚠️ **重要提醒：**

1. **获得同意**：录音他人通话前，请确保获得所有参与者的明确同意
2. **遵守法律**：请遵守您所在地区关于录音和隐私的法律法规
3. **合法用途**：请勿将录音用于非法目的
4. **自负责任**：使用本软件即表示您同意自行承担所有法律责任

不同地区的法律可能有所不同，请咨询法律专业人士。

## 技术支持

- **问题反馈**：[GitHub Issues](https://github.com/yourusername/auto-recorder/issues)
- **功能建议**：[GitHub Discussions](https://github.com/yourusername/auto-recorder/discussions)
- **文档**：[GitHub Wiki](https://github.com/yourusername/auto-recorder/wiki)

## 更新日志

查看 [CHANGELOG.md](CHANGELOG.md) 了解版本更新历史。

# 🎤 智能录音工具 (Smart Recorder)

一款智能的 Windows 桌面端录音工具,旨在自动检测并录制任何使用麦克风的应用程序。它能静默运行在后台,一旦检测到麦克风被占用,便会自动开始录制,并在通话结束后自动停止,为您无缝保留每一段重要对话。

## ✨ 主要功能

### 🎤 智能检测
- **实时监测**: 通过 Windows Core Audio API 持续监测系统麦克风使用情况
- **自动触发**: 全自动开始/停止录音,无需任何手动操作
- **进程识别**: 自动识别使用麦克风的应用程序

### 🎙️ 手动录音
- **一键录音**: 支持手动启动/停止录音功能
- **灵活控制**: 不依赖麦克风检测,随时可以录音
- **独立保存**: 手动录音文件自动标记为 manual_ 前缀

### 🎶 双通道录制
- **麦克风通道**: 同步捕获您的麦克风声音
- **扬声器通道**: 使用 WASAPI Loopback 捕获对方的扬声器声音
- **无需配置**: 不需要启用立体声混音设备
- **实时混音**: 确保通话双方的音频都被清晰录制

### 🎧 自动混音与编码
- **智能混音**: 录音结束后自动将双通道音频混音
- **MP3编码**: 使用 LAME 编码器生成高质量 MP3 文件
- **WAV格式**: 可选保存为无损 WAV 格式
- **可调音质**: 支持多种采样率 (16kHz - 96kHz) 和比特率 (64 - 320 kbps)
- **质量设置**: MP3 编码质量可调 (0-9 级别)

### ⚙️ 高度可定制

#### 音质配置
- **采样率选项**: 16000 / 22050 / 44100 / 48000 / 96000 Hz
- **比特率选项**: 64 / 96 / 128 / 192 / 256 / 320 kbps (MP3)
- **编码质量**: 0 (最高质量) 到 9 (最快速度) 可选
- **格式支持**: MP3 (有损压缩) 或 WAV (无损)

#### 设备与增益
- **独立增益**: 独立调节麦克风和扬声器的录音音量
- **平衡调整**: 平衡双方声音,确保清晰度
- **实时电平**: 提供可视化电平监测

#### 进程黑名单
- **智能过滤**: 可添加不希望触发录音的应用程序
- **常见应用**: 预设游戏、浏览器等黑名单
- **灵活配置**: 随时添加/删除黑名单应用

### 📂 文件管理

#### 智能归档
- **应用分类**: 录音文件根据应用名称自动创建文件夹
- **时间命名**: 以精确到秒的时间戳命名
- **自定义路径**: 自由设定录音文件保存位置

#### 文件格式
```
recordings/
├── QQ/
│   ├── QQ_20260208_143520.mp3
│   └── QQ_20260208_150130.mp3
├── WeChat/
│   ├── WeChat_20260208_091245.mp3
│   └── WeChat_20260208_153410.mp3
└── Teams/
    └── Teams_20260208_140055.mp3
```

### 🚀 便捷体验
- **开机自启**: 支持开机自动启动
- **系统托盘**: 最小化到托盘,静默运行
- **一次设置**: 永久省心,自动工作

### 📄 详细日志
- **运行日志**: 完整记录所有操作
- **实时显示**: GUI界面显示最近100条日志
- **便于调试**: 问题排查和性能监控

## 🛠️ 技术原理

### 1. 麦克风占用检测

通过 Windows Core Audio API (`IAudioSessionManager2`) 检测音频会话:

```rust
// 获取音频会话管理器
let session_manager: IAudioSessionManager2 = device.Activate(CLSCTX_ALL, None)?;

// 枚举所有活跃会话
let session_enumerator = session_manager.GetSessionEnumerator()?;
let count = session_enumerator.GetCount()?;

for i in 0..count {
    let session = session_enumerator.GetSession(i)?;
    let session2 = session.cast::<IAudioSessionControl2>()?;
    
    // 获取进程ID和状态
    let process_id = session2.GetProcessId()?;
    let state = session.GetState()?;
    
    if state == AudioSessionStateActive {
        // 检测到活跃的麦克风使用
    }
}
```

**工作流程**:
1. 枚举所有音频会话
2. 识别麦克风捕获会话 (`eCapture`)
3. 获取使用麦克风的进程ID
4. 查询进程名称
5. 过滤黑名单应用
6. 触发录音

### 2. 双通道音频捕获 (WASAPI)

#### 麦克风输入捕获
```rust
// 初始化麦克风捕获
let audio_client: IAudioClient = mic_device.Activate(CLSCTX_ALL, None)?;

audio_client.Initialize(
    AUDCLNT_SHAREMODE_SHARED,
    AUDCLNT_STREAMFLAGS_EVENTCALLBACK,
    10000000,  // 1秒缓冲
    0,
    pwfx,
    None,
)?;

let capture_client: IAudioCaptureClient = audio_client.GetService()?;
audio_client.Start()?;

// 捕获循环
while recording {
    capture_client.GetBuffer(&mut data_ptr, &mut num_frames, ...)?;
    // 处理麦克风数据
    process_microphone_data(data_ptr, num_frames);
    capture_client.ReleaseBuffer(num_frames)?;
}
```

#### 扬声器输出捕获 (Loopback模式)
```rust
// 使用 Loopback 标志捕获系统音频输出
audio_client.Initialize(
    AUDCLNT_SHAREMODE_SHARED,
    AUDCLNT_STREAMFLAGS_LOOPBACK,  // 关键: Loopback标志
    10000000,
    0,
    pwfx,
    None,
)?;

// 捕获系统播放的所有音频(包括对方声音)
while recording {
    capture_client.GetBuffer(&mut data_ptr, &mut num_frames, ...)?;
    // 处理扬声器数据
    process_speaker_data(data_ptr, num_frames);
    capture_client.ReleaseBuffer(num_frames)?;
}
```

**优势**:
- ✅ 无需立体声混音设备
- ✅ 捕获所有系统音频输出
- ✅ 低延迟、高音质
- ✅ 100% Windows兼容

### 3. 后期混音与MP3转码

#### 音频混音
```rust
pub struct AudioMixer {
    mic_buffer: Vec<f32>,
    speaker_buffer: Vec<f32>,
}

impl AudioMixer {
    pub fn mix(&mut self) -> Vec<f32> {
        let min_len = self.mic_buffer.len().min(self.speaker_buffer.len());
        
        let mixed: Vec<f32> = (0..min_len)
            .map(|i| {
                // 混音算法: 平均两个通道
                (self.mic_buffer[i] + self.speaker_buffer[i]) / 2.0
            })
            .collect();
        
        mixed
    }
}
```

#### MP3编码 (LAME)
```rust
use mp3lame_encoder::{Builder, InterleavedPcm};

// 创建LAME编码器
let mut encoder = Builder::new()?
    .set_sample_rate(48000)?
    .set_num_channels(1)?
    .set_brate(Bitrate::Kbps(128))?
    .set_quality(Quality::Best)?
    .build()?;

// 编码音频数据
let pcm_samples: Vec<i16> = f32_samples
    .iter()
    .map(|&s| (s.clamp(-1.0, 1.0) * 32767.0) as i16)
    .collect();

let input = InterleavedPcm(&pcm_samples);
let mp3_data = encoder.encode(input)?;
```

**编码流程**:
1. 实时混音麦克风和扬声器音频
2. 转换f32格式到i16 PCM
3. 使用LAME编码器压缩为MP3
4. 流式写入文件

## 📦 安装与使用

### 系统要求
- Windows 7 / 8 / 10 / 11
- Rust 1.70+ (编译时)
- 麦克风设备
- 音频输出设备

### 编译

#### 方法1: GUI版本(推荐)
```bash
cargo build --release --bin smart-recorder-gui
```

#### 方法2: CLI版本
```bash
cargo build --release --bin smart-recorder
```

### 运行

#### GUI模式
```bash
smart-recorder-gui.exe
```

#### CLI模式
```bash
# 启动智能监控
smart-recorder monitor

# 自定义输出目录
smart-recorder monitor --output "D:\Recordings"

# 设置黑名单
smart-recorder monitor --blacklist "chrome.exe,firefox.exe"

# 测试麦克风检测
smart-recorder test-detection
```

## 🎨 界面功能

### 主界面

#### 状态显示
- **监控状态**: 显示当前是否正在监控
- **录音状态**: 显示是否正在录音
- **当前应用**: 显示正在录音的应用名称

#### 控制按钮
- **开始/停止监控**: 启动或停止智能监控
- **打开文件夹**: 快速访问录音保存目录
- **设置**: 打开设置窗口

#### 实时音频电平
- **麦克风电平条**: 实时显示麦克风输入音量
- **扬声器电平条**: 实时显示系统播放音量
- **百分比显示**: 数值化显示当前电平

#### 运行日志
- 显示最近100条操作日志
- 自动滚动到最新日志
- 时间戳精确到秒

### 设置窗口

#### 基本设置
- **保存路径**: 自定义录音文件保存位置
- **自动创建文件夹**: 是否按应用名创建文件夹

#### 音质设置
- **采样率**: 44100 / 48000 Hz
- **音频格式**: MP3 / WAV
- **比特率**: 96 / 128 / 192 / 256 kbps (MP3)

#### 音量增益
- **麦克风增益**: 0.0x - 2.0x 可调
- **扬声器增益**: 0.0x - 2.0x 可调
- 实时滑块调节

#### 进程黑名单
- 多行文本框编辑
- 逗号分隔进程名
- 预设常见应用

#### 其他设置
- **最小录音时长**: 避免误触发
- **开机自启动**: 系统启动时自动运行
- **最小化到托盘**: 后台静默运行

## 🔧 配置文件

配置自动保存在 `smart_recorder_config.json`:

```json
{
  "output_path": "D:\\Recordings",
  "auto_create_folders": true,
  "sample_rate": 48000,
  "bit_rate": 128,
  "audio_format": "mp3",
  "mic_gain": 1.0,
  "speaker_gain": 1.0,
  "blacklist": "chrome.exe,firefox.exe,msedge.exe,explorer.exe",
  "auto_start": false,
  "minimize_to_tray": true,
  "min_duration_seconds": 3
}
```

## 📊 性能指标

| 指标 | 数值 |
|------|------|
| CPU占用 | <2% (监控模式) |
| 内存占用 | ~50MB |
| 录音延迟 | <100ms |
| 检测间隔 | 1秒 |
| 音频缓冲 | 10ms |

## 🐛 故障排除

### 问题: 无法检测到麦克风使用
**解决**:
1. 确保程序有管理员权限
2. 检查Windows隐私设置 → 麦克风权限
3. 查看运行日志是否有错误

### 问题: 扬声器通道无声音
**解决**:
1. 确保有音频输出设备
2. 播放一段音乐测试
3. 检查扬声器增益设置

### 问题: 录音文件过大
**解决**:
1. 使用MP3格式代替WAV
2. 降低比特率(96 kbps足够语音)
3. 调整最小录音时长

### 问题: 某些应用触发录音
**解决**:
1. 将应用添加到黑名单
2. 格式: `应用名.exe`,逗号分隔
3. 保存设置并重启监控

## 🌟 使用场景

### 在线会议
- Zoom / Teams / 钉钉 / 腾讯会议
- 自动记录重要会议内容
- 双通道清晰保存

### 客服录音
- 电话客服系统
- 质量监控和培训
- 自动归档

### 在线教育
- 录制网课内容
- 一对一辅导记录
- 方便课后复习

### 远程面试
- HR面试录音
- 技术面试记录
- 合规存档

## 📚 技术栈

### 核心技术
- **Rust**: 高性能系统编程语言
- **Windows API**: 原生系统接口
- **WASAPI**: Windows音频会话API
- **LAME**: 高质量MP3编码器

### 主要库
- `windows-rs`: Windows API绑定
- `cpal`: 跨平台音频库
- `mp3lame-encoder`: LAME编码器绑定
- `egui/eframe`: 即时模式GUI框架
- `crossbeam-channel`: 高性能通道
- `parking_lot`: 快速互斥锁

## 📦 自动发布

本项目使用 GitHub Actions 实现自动编译和发布:

### 发布新版本

1. 更新 `Cargo.toml` 中的版本号
2. 提交更改并创建标签:
   ```bash
   git add .
   git commit -m "Release v1.1.0"
   git tag v1.1.0
   git push origin main --tags
   ```
3. GitHub Actions 会自动:
   - 编译 Windows 版本
   - 创建 Release
   - 上传可执行文件和文档

### 手动触发发布

也可以在 GitHub Actions 页面手动触发工作流。

## 📝 开发计划

### v1.1 ✅ (已完成)
- [x] 手动录音功能
- [x] 可配置采样频率 (16kHz - 96kHz)
- [x] 可配置比特率 (64 - 320 kbps)
- [x] MP3 编码质量设置 (0-9)
- [x] GitHub Actions 自动发布

### v1.2
- [ ] 系统托盘功能
- [ ] 热键控制
- [ ] 多语言支持

### v1.3
- [ ] 音频波形可视化
- [ ] 录音片段剪辑功能
- [ ] 录音文件搜索

### v2.0
- [ ] 云端同步
- [ ] 语音转文字
- [ ] 智能标签
- [ ] 实时降噪
- [ ] AI摘要

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request!

---

**重要提示**: 请确保在录音前告知对方并获得同意,遵守当地法律法规。
